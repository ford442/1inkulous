{
  "name": "1inkulous",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "npm run build:wasm && vite",
    "dev:web": "vite",
    "build": "npm run build:wasm && tsc && vite build",
    "build:web": "tsc && vite build",
    "build:wasm": "bash scripts/build-wasm.sh",
    "clean:wasm": "rm -rf cpp/build-wasm src/wasm/core.js src/wasm/core.wasm",
    "preview": "vite preview",
    "test:core": "node scripts/test-core.mjs"
  },
  "devDependencies": {
    "@webgpu/types": "^0.1.71",
    "typescript": "~6.0.2",
    "vite": "^8.1.1"
  }
}
